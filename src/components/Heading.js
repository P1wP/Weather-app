import React from "react";

function Heading({heading}){

    return(
        <div className="Heading">
            <h1 className="Heading__text">{heading}</h1>
        </div>
    )
    
}

export default Heading;